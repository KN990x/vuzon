.git
.github
.vscode
.idea

**/node_modules
**/dist

# The backend test suite is never run inside the image, and `pnpm deploy` already excludes
# it from /prod via the `files` field. It was still being copied into the build context and
# into the build layer on every build — several thousand lines of it.
src/server/tests

npm-debug.log*
pnpm-debug.log*
yarn-error.log*

.env
.env*
!.env.example
.session_secret
sessions
# Local panel state (credential hash + session key): never part of the image
data

.DS_Store
Thumbs.db
*.swp

# Documentation and README images: none of this belongs in the image.
docs
README.md
CONTRIBUTING.md
LICENSE
*.md

Dockerfile
docker-compose*
.dockerignore
