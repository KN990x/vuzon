# Dependencies (pnpm workspace: the real node_modules at the root + per-package symlinks)
node_modules

# Build
dist

# Logs
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# Secrets and session state
.env
.env.*
!.env.example
.session_secret
sessions
# Panel credentials (hashed) + session signing key, written at runtime
data/

# Homelab / deploy secrets that tend to appear at the repo root
.npmrc
.envrc
.direnv/
docker-compose.override.yml
compose.override.y*ml
*.pem
*.key
*.crt
*.p12
*.pfx
id_rsa*

# Build / pack artefacts
*.tgz
coverage/
*.tsbuildinfo

# Editor and operating system
.DS_Store
Thumbs.db
*.swp
*~
*.bak
*.orig
*.rej
._*
*.local
.idea
.vscode

# Agent tooling: local guides, kept out of the public repository
.claude/
AGENTS.md
CLAUDE.md
